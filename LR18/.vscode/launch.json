{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Запуск C++",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/main",
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "preLaunchTask": "C/C++: g++ build active file"
        }
    ]
}
